import ApplicationAdapter from './application';

export default ApplicationAdapter.extend({
//   createRecord(store, type, record) {
//   // in this instance, the adapter is actually 'this'
//   const api = this.get('host')
//   const serialized = this.serialize(record)
//   console.log('record is', record)
//   const url = `${api}/campaigns`
//   const data = {campaign: {
//     'name': record.record.name,
//     'completed': record.record.completed
//   }}
//   return this.ajax(url, 'POST', {data})
// },
//   UpdateRecord(store, type, record) {
//     const api = this.get('host')
//     const serialized = this.serialize(record, {includeId: true})
//     console.log('record is', record)
//     const campaign_id = record.record.id
//     const url = `${api}/campaigns/${campaign_id}`
//     const data = {campaign: {
//       'name': record.record.name,
//       'completed': record.record.completed
//     }}
//     return this.ajax(url, 'POST', {data})
//   },
});
